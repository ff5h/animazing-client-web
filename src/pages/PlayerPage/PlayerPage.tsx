import React from 'react';

import styles from './PlayerPage.module.scss';

type PlayerPageProps = {};

const PlayerPage = ({}: PlayerPageProps): JSX.Element => {
  return <div className={styles.PlayerPage}></div>;
};

export default PlayerPage;
